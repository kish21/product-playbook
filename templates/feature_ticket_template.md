---
name: Feature Implementation Ticket
about: One scope-bounded engineering ticket generated by /tickets (Plan -> Contracts -> Ticket)
title: "[M<milestone>-<EPIC>-<nn>] "
labels: ["enhancement", "product-playbook"]
---

<!--
  ONE ticket = ONE concern = ONE pull request.
  Planned ticket [M<n>-<EPIC>-<nn>] : ticket nn of epic <EPIC> in milestone n, e.g. [M1-PARTY-02]. Its epic is
                                     the parent issue [M<n>-<EPIC>]; TICKETS.md holds the plan.
    VERTICAL slice                  : one thin user-observable behaviour, crossing layers on purpose.
    HORIZONTAL slice                : one architectural layer of one milestone.
  Ad-hoc         [ADHOC-<nn>]       : a bug, edge case, tech debt or security item found mid-build.
  If a reviewer would need a second concern to approve this, it is two tickets. Split it.
  A ticket file in docs/issues/ IS the issue body: it starts at this comment, never copies the front matter
  above, and is published unchanged with `gh issue create --body-file`.
-->

### 🎯 Goal
<!-- The milestone this serves, and the one concern THIS ticket owns. For an ad-hoc issue: what happened, expected vs actual. -->

### 🧭 Slice Strategy
<!-- Exactly one. Recorded so a later reader knows why the backlog is shaped this way. -->
- [ ] ↕️ Vertical — thin end-to-end increment, demoable on merge
- [ ] ↔️ Horizontal — single architectural layer
- [ ] 🐛 Ad-hoc — bug · edge-case · tech-debt · security

<!-- Epic (next heading): the epic this ticket belongs to, as [M<n>-<EPIC>] and its name. It is published as this
     issue's parent, and every ticket of an epic sits in the epic's lane. Empty for an ad-hoc issue. -->
### 🗂️ Epic


<!-- Lane (next heading): the MODULE this ticket lives in - a folder STRUCTURE.md draws ("parties", "notify"), never a
     technology layer ("backend", "ui"). One lane = one owner; the lane's tickets go in order, lanes run side by side.
     This comment sits ABOVE the heading so an empty field stays empty. -->
### 🛣️ Lane


<!-- Owner (next heading): the ROLE that owns the lane - Senior or Junior. Follows the files, not the difficulty.
     Senior unless the Delivery Board says otherwise; the board's Seat field says who actually sits there today. -->
### 👤 Owner


### 🧱 Layer(s)
<!-- Horizontal: exactly one. Vertical: every layer this slice touches. -->
- [ ] 📦 Storage / data provider — schema models, persistence adapter, migration
- [ ] ⚙️ Pure domain service — business logic, state transitions, validation
- [ ] 🎨 UI component — isolated component, interactive states, a11y, `DESIGN.md` tokens
- [ ] 🧪 Cross-layer verification — integration across the real seam, adversarial/security

### 📁 Target Files
<!-- EXACT paths from STRUCTURE.md, one backticked path per line: a file such as src/services/quoteEngine.ts,
     never a bare folder such as src/services/. This list is the BOUNDARY: it must name everything the
     build will write, the feature doc and the test files included.
     Never list PRODUCT.md, CHANGELOG.md, STRUCTURE.md or TICKETS.md here: the spine and the plan are shared and each phase writes its own rows. -->
- [ ] `path/to/file.ext`
- [ ] `docs/features/<feature>.md`

### 🔌 Contract — Inputs → Outputs
<!-- The typed shape this ticket consumes, and the one it exposes for dependants. Named from PRODUCT.md#Contracts. -->
- **Consumes:**
- **Exposes:**

### 🔗 Depends On
<!-- Ticket IDs whose MERGE this waits for. Each becomes a GitHub "blocked by" link, so /build will not start this ticket
     while one is open (a solid arrow in TICKETS.md). "None" is a valid answer. -->

### 🧩 Builds Against
<!-- Ticket IDs whose CONTRACT this uses before they merge: the types are frozen in #Contracts, so a stub is enough to
     start (a dotted arrow in TICKETS.md). Never a "blocked by" link. "None" is a valid answer. -->

### 👀 Demo — what works after this merges
<!-- REQUIRED on a vertical slice. If the honest answer is "nothing visible yet", this is a layer, not a slice: re-slice it or move the milestone to horizontal. Horizontal tickets: state the seam a reviewer can exercise. -->

### 🛠️ Implementation Tasks
<!-- Ordered, checkable, inside THIS ticket's concern only. -->
- [ ] 1.
- [ ] 2.

### 🔒 Definition of Done (security included)
- [ ] Inputs validated at the boundary (no injection, no raw HTML, no unvalidated payloads).
- [ ] No secrets in source — config and credentials in `.env` only.
- [ ] No swallowed errors; fallbacks fail **closed** on anything auth- or security-related.
- [ ] Structured logging, no stray prints on production paths.
- [ ] Tests written and passing for everything this ticket touches (for a bug fix: a **regression test that fails without the fix**).
- [ ] Feature doc written/updated at `docs/features/<feature>.md` (listed in Target Files above).
- [ ] **Mergeable alone** — a PR containing only these files is reviewable on its own, and the PR carries its `lane: <name>` label.

### 🧪 Verification Command
<!-- The exact command a reviewer runs to see this work. -->
```bash
```
