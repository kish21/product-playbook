{
  "skill_collection": "product-playbook",
  "note": "2-3 trigger/behaviour checks per skill (skill-creator convention). Each expected_output names the PRODUCT.md section + exit criteria the skill must satisfy. Re-run on change to catch drift from VISION.md.",
  "evals": [
    { "id": 0,  "skill": "playbook",    "prompt": "guide me through building a product", "expected_output": "Orients from PRODUCT.md (empty -> proposes /vision; partial -> first unfilled phase); explains the journey in plain language; runs ONE phase at a time, pausing at each gate for confirmation; never skips a gate or auto-builds; writes nothing itself", "files": [] },
    { "id": 1,  "skill": "vision",      "prompt": "I have an idea for a product, help me start", "expected_output": "Asks who/problem/why-now + north-star metric + riskiest assumption + business model; flags AI-or-not; benchmarks to current-year market with REAL named competitors (not from memory) + a job-to-be-done; writes PRODUCT.md#Vision with all fields; refuses to hand off on a fuzzy vision", "files": [] },
    { "id": 2,  "skill": "scope",       "prompt": "we keep adding features, what should the MVP be", "expected_output": "Forces exactly one core feature; sorts temptations into Deferred (with trigger) vs Non-goals (never), prompting common creep categories; ties items to a customer outcome + the north-star metric; writes PRODUCT.md#Scope; warns if #Vision missing", "files": [] },
    { "id": 3,  "skill": "plan",        "prompt": "make a roadmap for this", "expected_output": "Core-first milestones, each with a testable exit criterion + rough timeline + a concern-area coverage checklist (security/observability/testing/docs/DX) marked now/next/later; writes PRODUCT.md#Plan", "files": [] },
    { "id": 4,  "skill": "architect",   "prompt": "what tech stack should we use", "expected_output": "Picks stack benchmarked to 2026 OSS with one-line whys; names an adapter interface per external; names the design patterns to apply + anti-patterns to avoid (current-year), adapted to the project; chooses a migrations approach; records ADRs; writes PRODUCT.md#Architecture", "files": [] },
    { "id": 5,  "skill": "structure",   "prompt": "set up the project folder structure", "expected_output": "Asks backend/frontend/full-stack; lays the layered tree (incl. config/ loader+yaml, db/) + root scaffolding (.gitignore ignoring .env*, .gitleaks.toml, pre-commit, .env.example, Makefile, dep manifest) + (AI) prompts/ YAML; no secret in code; writes STRUCTURE.md explaining each folder + PRODUCT.md#Structure", "files": [] },
    { "id": 6,  "skill": "foundation",  "prompt": "get a basic skeleton running with CI", "expected_output": "Runnable health path (shown); config loader with fail-loud/fail-closed guard; structured logging + tracing/error-reporter stub; pre-commit + CI auto-layer running secret-scan + dependency-vuln + tests and blocking on red; runs in its container; runtime-generated creds; async-safe; verifies a config value actually flows; writes PRODUCT.md#Foundation", "files": [] },
    { "id": 7,  "skill": "contracts",   "prompt": "define the data models and schema", "expected_output": "Typed models + migration that applies; schema↔code consistent; agrees units/scale at each boundary; contract versioning/back-compat; PII classified + tenant + idempotency keys; writes PRODUCT.md#Contracts", "files": [] },
    { "id": 8,  "skill": "build",       "prompt": "build the login feature", "expected_output": "Declares security-in-DoD; reuse scan; verifies the LIVE path (not just units); /code-review on diff; prompts in YAML; no secret in code (fake keys in tests); single-responsibility (splits god-files); writes docs/features/* + PRODUCT.md#Build log with how-verified; flags if feature is OUT OF SCOPE", "files": [] },
    { "id": 9,  "skill": "dev-check",   "prompt": "is development complete, ready to test", "expected_output": "Verifies each core feature runs + met exit criteria + security DoD with evidence; scope re-check; FAILS the gate (stops) if any box unmet; writes PRODUCT.md#Dev-complete", "files": [] },
    { "id": 10, "skill": "test",        "prompt": "write tests for this", "expected_output": "Unit (isolated) + integration (real contracts) + live-path + adversarial/security (injection for AI, authz) + a golden/eval dataset; fake keys only; writes PRODUCT.md#Tests; refuses if only isolated units", "files": [] },
    { "id": 11, "skill": "eval",        "prompt": "is the output actually good", "expected_output": "Defines a measurable 'good'; measures against representative inputs; separates operational failures from quality; cost-per-run (+ AI bias check); emits a confidence score; writes PRODUCT.md#Evaluation", "files": [] },
    { "id": 12, "skill": "ship",        "prompt": "ship this feature and open a PR", "expected_output": "Deep /code-review (traces callers) + /security-review + /doc-audit reconcile + CHANGELOG + security checklist (dep-vuln, CORS, cookie-auth, data-deletion) + confidence score + github-pr-flow PR + handoff; STOPS if a doc claim doesn't match code; writes PRODUCT.md#Ship log", "files": [] },
    { "id": 13, "skill": "learn",       "prompt": "the feature shipped, what next", "expected_output": "Captures the success metric result; short retro; decides next FROM EVIDENCE re-checked against vision + OUT-OF-SCOPE; writes PRODUCT.md#Learnings", "files": [] },
    { "id": 14, "skill": "drift-check", "prompt": "are we still on track or did we drift", "expected_output": "Cross-checks built features vs Deferred/Non-goals (creep) + the inverse (unrecorded pivot), direction vs vision+metric, milestones + the concern-area checklist, and code↔docs via /doc-audit; honest verdict; records confirmed drift in PRODUCT.md#Drift log (not #Learnings); does not advance the chain", "files": [] },
    { "id": 15, "skill": "drift-check", "prompt": "are we on track? (existing project: has CLAUDE.md + README, but NO PRODUCT.md)", "expected_output": "Tier-2 spine resolution (PRINCIPLES.md): with no PRODUCT.md it resolves the spine from project docs (CLAUDE.md -> README -> docs/ -> AGENTS.md) and STATES which file it used as the spine; runs the normal drift check against that; on confirmed drift it reports to the user / offers to append to the project's log, and does NOT silently create a PRODUCT.md#Drift log", "files": [] },
    { "id": 16, "skill": "drift-check", "prompt": "are we on track? (code-only repo: source + package.json, NO spine doc at all)", "expected_output": "Tier-3 spine resolution (PRINCIPLES.md): emits a clearly-labelled INFERRED low-confidence read from code + package metadata; states plainly that true scope/vision drift CANNOT be judged with no recorded intent; recommends bootstrapping a spine (/vision+/scope or a minimal PRODUCT.md/CLAUDE.md); must NOT fabricate a baseline, must NOT report 'on-track', must NOT create a PRODUCT.md", "files": [] }
  ]
}
